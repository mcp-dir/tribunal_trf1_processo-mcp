# Ferramentas

Tribunal TRF1: Processo expõe 1 ferramenta (todas somente leitura).

### 1. `tribunal_trf1_processo_consultar`
**Input**: `processo` (opcional), `parte` (opcional), `cpf` (opcional), `cnpj` (opcional), `advogado` (opcional), `oab` (opcional)

Tribunal TRF1: Processo, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Tribunal TRF1: Processo
```
